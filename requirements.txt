# Core dependencies for both methods
markdown==3.5.2

# ReportLab dependencies (for macOS/platform-independent solution)
reportlab==4.3.1
pillow==11.1.0

# pdfkit dependencies (for Windows solution with wkhtmltopdf)
pdfkit==1.0.0